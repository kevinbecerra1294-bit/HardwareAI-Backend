{
  "version": 2,
  "builds": [
    {
      "src": "Dockerfile",
      "use": "@vercel/node" 
    }
  ],
  "routes": [
    {
      "src": "/(.*)", // Captura TODAS las rutas entrantes
      "dest": "/"     // Redirige TODAS las rutas al contenedor Docker
    }
  ]
}
